Put all triangles on individual UV coords on single texture

Create texture of normal vectors -> dtex_nrm

Create texture of world position -> dtex_pos

Create visibility texture -> dtex_vis

Create empty lightmap texture -> lmap

Pass 1:
	Write *ONLY* the emissive component into lightmap

Further Passes:
	Do Radiosity
